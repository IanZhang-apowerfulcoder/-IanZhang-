# Agent Runtime

Owner: P2. Implements `project/learning_workflow.yaml`, Agent Registry, parallel groups, RAG Router, checkpoint, retry, timeout and Arbitration. Agent modules are in-process/plugin modules in v1, not one HTTP service per Agent.
