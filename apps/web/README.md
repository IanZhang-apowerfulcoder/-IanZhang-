# Web Frontend

Owner: P4. Build learner, admin and observability pages from `api/openapi.yaml`. Generate TypeScript clients; do not hand-maintain a second contract. Real-time status uses SSE and event replay.
