# Business API

Owner: P5. Public API, auth, tenant checks, business state, idempotency, Agent job creation, output validation, authoritative writes and frontend view adaptation. Development runs locally; later deployment uses the same service in a container.
