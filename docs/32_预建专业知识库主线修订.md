# 预建专业知识库主线修订

比赛主流程只使用已发布 Domain Pack，不依赖运行时自动建库。当前 Domain Pack v2 合并两份专业 AI 知识包，默认可检索；第二份内容保留 `pending_team_review` 标记，后续由队员持续复核和扩充。

知识更新链：结构化导入 → candidate/review → validate → reindex → regression → publish → activate。它由管理员工具和脚本完成，不作为学习 Agent。
