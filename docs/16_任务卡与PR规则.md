# 任务卡与 PR 规则

任务状态：`BACKLOG → READY → IN_PROGRESS → IN_REVIEW → ACCEPTED`，异常为 `BLOCKED / REWORK / CANCELLED`。

任务卡必须包含目标、范围、禁止范围、依赖、必读文件、API/Schema、交付物、量化验收、证据、复核人和回滚方案。

成员提交“完成”时必须附：PR、测试日志、接口样例/截图、验收自检、已知限制。无证据不能进入 ACCEPTED。
