# 成员 API 与 Mock 冻结总表

机器可读清单位于：

- `project/api_ownership.yaml`；
- `project/member_reference_matrix.yaml`；
- `members/Px/openapi_scope.yaml`；
- `members/Px/internal_scope.yaml`；
- `members/Px/mock_index.yaml`。

任何成员只消费自己范围内的接口；需要跨范围必须提交任务依赖或接口变更。
