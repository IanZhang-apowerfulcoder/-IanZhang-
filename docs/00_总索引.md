# 文件总索引

## 最先阅读

- `README.md`：V6入口和冻结原则。
- `docs/28_九成员开发文件总教程.md`：所有成员的统一教程。
- `docs/03_成员必读文件矩阵.md`：进入个人开发目录。
- `members/`：九名成员各自的API、内部接口和Mock入口。

## 核心契约

- `api/openapi.yaml`：66个公开接口。
- `api/internal_agent_interfaces.yaml`：6个智能体接口。
- `api/error_codes.yaml`：统一错误码。
- `api/field_dictionary.yaml`：冻结字段词典。
- `contracts/jsonschema/agents/`：智能体输入输出Schema。
- `contracts/jsonschema/services/`：内部服务交接Schema。
- `contracts/typescript/openapi-contracts.generated.ts`：完整前端类型。
- `database/schema.sql`：V6数据库基线。

## 责任与流程

- `project/api_ownership.yaml`：每个接口的契约负责人、实现人和消费人。
- `project/boundary_contract_manifest.yaml`：27个跨模块边界。
- `project/member_contracts/`：九人的机器可读责任文件。
- `project/user_flows.yaml`：三条机器可读用户流程。
- `project/module_path_owners.yaml`：代码目录权限范围。

## Mock

- `mocks/http/`：66个公开接口的请求、成功和错误Mock。
- `mocks/internal/agents/`：六智能体输入输出Mock。
- `mocks/internal/services/`：内部服务交接Mock。
- `mocks/workflows/`：完整流程Mock。

## 检查

- `scripts/validate_v6_contracts.py`：自动契约检查。
- `reports/validation_report.md`：最新检查结果。

## 预建专业知识库

- `knowledge-bases/enterprise-rag-engineering-v1/`：企业级RAG工程Domain Pack完整内容、服务、评测和提交样例。
- `api/knowledge_domain_api.yaml`：知识包独立服务参考接口。
- `database/migrations/002_enterprise_rag_domain_pack.sql`：知识节点、切片、题库和评测表扩展。
- `docs/32_预建专业知识库主线修订.md`：主闭环修订决策。
- `docs/33_企业级RAG知识库接入与验收.md`：接入步骤和验收门禁。


- `docs/34_知识库交付清单与团队接入.md`：预建知识库的团队接入、验收和变更规则。
