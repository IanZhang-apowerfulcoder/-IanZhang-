# 九成员独立开发文件说明

每个 `members/Px_姓名/` 目录包含：

- `00_开始这里.md`；
- `00_完整开发文件.md`；
- `openapi_scope.yaml`；
- `internal_scope.yaml`；
- `mock_index.yaml`。

`member-packs-v7.0/` 提供可单独发给成员的压缩包，但 GitHub 主仓库仍是唯一事实来源。
