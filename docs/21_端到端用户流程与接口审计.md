# 端到端用户流程与接口审计

主流程从“创建学习会话”到“更新画像”必须逐步映射到 Public API、Internal API、Agent Schema、数据库表和前端页面。审计表见 `project/end_to_end_contract_matrix.yaml`。

任何一步如果只有文档描述、没有 operationId/Schema/Mock/表或事件之一，应标记为契约缺口，不能进入真实开发。
