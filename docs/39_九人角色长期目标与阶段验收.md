# 九人角色长期目标与阶段验收

长期角色决定模块所有权，阶段任务决定当前工作范围。每个成员在 `members/` 中有长期文件，在 `tasks/Sx/` 中有 8 张任务卡。阶段验收以交付物、自动测试和证据为准，不以“写完了”或“页面能打开”作为通过条件。

P9 是独立质量 Owner，但其 Review Agent 由 P1、P3、P7 联合验收，避免自己验收自己。P1 不替成员决定模块内部全部实现，但拥有 Agent 节点、边、权限、输入输出和最终质量门禁的决定权。
