# 陈严谨完整开发文件

> 成员编号：P7  
> 角色：管理端、知识库页面与明确范围的简单接口  
> 能力层级：成长型成员  
> 契约基线：`frozen_v6`  
> 本文件版本：`V6.1`（只增强成员说明，不改变 V6 API、字段和 Schema）

## 1. 先理解：你到底根据什么开发

本文件把与你有关的接口、字段、Mock、开发阶段和禁止事项集中到一处，便于执行。但发生冲突时，按以下优先级判断：

1. `api/openapi.yaml`：前端与后端 HTTP 接口的唯一事实来源。
2. `api/internal_agent_interfaces.yaml`：六智能体函数、超时、重试和 Schema 路径的唯一事实来源。
3. `contracts/jsonschema/`：智能体与内部服务跨模块 JSON 字段的唯一事实来源。
4. `api/field_dictionary.yaml`：核心标识符含义与负责人。
5. `api/error_codes.yaml`：统一错误码。
6. `database/schema.sql`：数据库持久化结构。
7. `mocks/`：开发和联调样例；Mock 不能覆盖正式契约。
8. 本文件：上述正式契约的成员化说明和执行教程。

**模块内部变量可以自定义；只要数据离开本模块，就必须转换为本文件和正式契约规定的字段。**

## 2. 你的职责与管理关系

- 直接指导人：李佳乐
- 代码审核人：李佳乐、陈尧
- 你负责的角色：管理端、知识库页面与明确范围的简单接口
- 契约状态：`frozen_v6`

### 允许修改的代码范围

- `apps/web/src/features/admin/**`
- `services/knowledge/api/**`
- `services/training/api/**`
- `tests/admin/**`

### 明确禁止

- 修改多租户权限规则
- 修改知识构建算法
- 修改数据库核心表
- 修改公共契约
- 不得删除、重命名或改变冻结字段类型。
- 不得把大模型原始文本直接穿透给前端。
- 不得自行增加第二套接口、第二套 Mock 或第二套公共类型。
- 字段不够用时必须提交 `templates/API_CHANGE_REQUEST.md`，不能先改代码。

## 3. 开发前阅读顺序

1. `README.md`
2. `CONTRIBUTING.md`
3. `docs/01_项目重点与范围.md`
4. `docs/06_系统模块边界.md`
5. `docs/10_命名与字段契约.md`
6. `docs/11_接口所有权与变更流程.md`
7. `docs/14_测试与验收标准.md`
8. `docs/15_AI辅助开发约束.md`
9. `docs/16_任务卡与PR规则.md`
10. `docs/21_端到端用户流程与接口审计.md`
11. `docs/22_跨模块数据传输契约矩阵.md`
12. `docs/23_标识符与关联键生命周期.md`
13. `docs/25_异步任务轮询与状态规则.md`
14. `docs/26_字段冻结清单.md`
15. `docs/28_九成员开发文件总教程.md`
16. `docs/12_前端页面接口依赖.md`
17. `api/openapi.yaml`
18. `project/boundary_contract_manifest.yaml`
19. `docs/25_异步任务轮询与状态规则.md`

然后阅读本目录中的：

1. `openapi_scope.yaml`
2. `internal_scope.yaml`
3. `mock_index.yaml`
4. 本文件后面的逐接口说明

## 4. 你的接口和内部接口总览

### 公开 HTTP 接口

- `archive_document`：归档文档
- `archive_knowledge_base`：归档知识库
- `create_assignments`：批量分配学习者
- `create_knowledge_base`：创建知识库
- `create_training_program`：创建培训项目
- `get_assignment`：获取培训分配
- `get_build_run`：获取知识构建任务
- `get_document`：获取文档
- `get_knowledge_base`：获取知识库
- `get_knowledge_base_version`：获取知识库版本
- `get_knowledge_graph`：获取知识图谱摘要
- `get_learner`：获取学习者
- `get_quality_report`：获取知识库质量报告
- `get_training_program`：获取培训项目
- `list_build_events`：查询知识构建事件
- `list_build_runs`：查询知识构建任务
- `list_documents`：查询知识库文档
- `list_knowledge_base_versions`：查询知识库版本
- `list_knowledge_bases`：查询知识库
- `list_learner_assignments`：查询学习者培训任务
- `list_learners`：查询学习者
- `list_program_assignments`：查询培训项目分配
- `list_training_programs`：查询培训项目
- `publish_training_program`：发布培训项目
- `retry_document_parse`：重试文档解析
- `update_knowledge_base`：更新知识库
- `update_training_program`：更新培训项目
- `upload_document`：上传文档
- `activate_knowledge_base_version`：激活知识库版本
- `cancel_build_run`：取消知识构建任务
- `create_build_run`：发起知识构建
- `get_current_user`：获取当前用户
- `get_human_review_task`：获取人工审核任务
- `get_organization`：获取组织信息
- `list_human_review_tasks`：查询人工审核任务
- `login`：用户登录
- `publish_knowledge_base_version`：发布知识库版本
- `run_retrieval_test`：运行检索评测

### 智能体内部接口

- 无智能体内部接口

## 5. 你必须使用的核心跨模块变量名

下面只列出与你当前接口和智能体范围有关、且已经进入核心字段词典的冻结字段。字段拼写、类型和语义均不得自行修改。

| 字段 | 类型 | 含义 | 负责人 |
|---|---|---|---|
| `organization_id` | `uuid` | 组织租户标识 | 张英赫 |
| `knowledge_base_id` | `uuid` | 逻辑知识库标识 | 李佳乐 |
| `knowledge_base_version_id` | `uuid` | 不可变知识库版本标识 | 李佳乐 |
| `document_id` | `uuid` | 上传文档标识 | 李佳乐 |
| `build_run_id` | `uuid` | 知识构建任务标识 | 李佳乐 |
| `learner_id` | `uuid` | 学习者标识 | 杨欣怡 |
| `training_program_id` | `uuid` | 培训项目标识 | 杨欣怡 |
| `assignment_id` | `uuid` | 培训分配标识 | 杨欣怡 |
| `session_id` | `uuid` | 一次完整学习会话标识 | 张英赫 |
| `decision_cycle_id` | `uuid` | 会话内一轮联合决策标识 | 张英赫 |
| `evidence_id` | `uuid` | 可追溯证据标识 | 李佳乐 |
| `idempotency_key` | `string` | 避免重复创建或重复提交的幂等键 | 张英赫 |

此外，所有公开接口统一遵守：

- JSON 字段：`snake_case`
- API 路径：小写英文加短横线
- TypeScript 类型：`PascalCase`
- 数据库字段：`snake_case`
- 枚举值：`lower_snake_case`
- 时间：ISO 8601 UTC 字符串
- 标识符：UUID 字符串
- 所有创建、提交类请求优先携带 `idempotency_key`
- 同一联合决策链路使用同一个 `correlation_id`

## 6. 逐个公开 API 开发说明

### `archive_document` — 归档文档

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`DELETE /api/v1/organizations/{organization_id}/documents/{document_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`无 JSON 响应体`**

统一响应外壳：

无

响应 `data` 中的主要跨模块字段：

无

**必须使用的 Mock**

- `mocks/http/archive_document/request.json`
- `mocks/http/archive_document/response.json`
- `mocks/http/archive_document/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `archive_knowledge_base` — 归档知识库

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`DELETE /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`无 JSON 响应体`**

统一响应外壳：

无

响应 `data` 中的主要跨模块字段：

无

**必须使用的 Mock**

- `mocks/http/archive_knowledge_base/request.json`
- `mocks/http/archive_knowledge_base/response.json`
- `mocks/http/archive_knowledge_base/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `create_assignments` — 批量分配学习者

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/training-programs/{training_program_id}/assignments`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |

**请求体结构：`CreateAssignmentsRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `learner_ids` | 是 | `array<string(uuid)>` |  |  |  |
| `due_at` | 否 | `string \| null(date-time)` |  |  |  |
| `idempotency_key` | 是 | `string` |  | 避免重复创建或重复提交的幂等键 | 张英赫 |

**成功响应结构：`ApiResponseLearnerAssignmentList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<LearnerAssignmentView>` |  |  |  |
| `items[].assignment_id` | 是 | `string(uuid)` |  | 培训分配标识 | 杨欣怡 |
| `items[].organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `items[].training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `items[].learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `items[].assignment_status` | 是 | `string` | assigned, in_progress, completed, cancelled, expired |  |  |
| `items[].due_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].assigned_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `items[].assigned_at` | 是 | `string(date-time)` |  |  |  |
| `items[].started_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].completed_at` | 否 | `string \| null(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/create_assignments/request.json`
- `mocks/http/create_assignments/response.json`
- `mocks/http/create_assignments/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `create_knowledge_base` — 创建知识库

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/knowledge-bases`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |

**请求体结构：`CreateKnowledgeBaseRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `name` | 是 | `string` |  |  |  |
| `description` | 否 | `string \| null` |  |  |  |

**成功响应结构：`ApiResponseKnowledgeBase`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeBaseView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `name` | 是 | `string` |  |  |  |
| `description` | 否 | `string \| null` |  |  |  |
| `status` | 是 | `string` | draft, building, ready, failed, archived |  |  |
| `document_count` | 是 | `integer` |  | 文档数 |  |
| `active_version` | 否 | `integer \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  | 更新时间 |  |
| `active_version_id` | 否 | `string \| null(uuid)` |  | 当前激活版本 |  |
| `active_version_no` | 否 | `integer \| null` |  | 当前激活版本号 |  |

**必须使用的 Mock**

- `mocks/http/create_knowledge_base/request.json`
- `mocks/http/create_knowledge_base/response.json`
- `mocks/http/create_knowledge_base/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `create_training_program` — 创建培训项目

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/training-programs`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |

**请求体结构：`CreateTrainingProgramRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `program_name` | 是 | `string` |  |  |  |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `target_description` | 是 | `string` |  |  |  |

**成功响应结构：`ApiResponseTrainingProgram`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `TrainingProgramView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `program_name` | 是 | `string` |  |  |  |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `program_status` | 是 | `string` | draft, published, archived |  |  |
| `target_description` | 是 | `string` |  |  |  |
| `created_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/create_training_program/request.json`
- `mocks/http/create_training_program/response.json`
- `mocks/http/create_training_program/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_assignment` — 获取培训分配

- 你的关系：**实现负责人**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/assignments/{assignment_id}`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：X、斯汀
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:assignment_id` | 是 | `string(uuid)` |  | 培训分配标识 | 杨欣怡 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseLearnerAssignment`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `LearnerAssignmentView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `assignment_id` | 是 | `string(uuid)` |  | 培训分配标识 | 杨欣怡 |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `assignment_status` | 是 | `string` | assigned, in_progress, completed, cancelled, expired |  |  |
| `due_at` | 否 | `string \| null(date-time)` |  |  |  |
| `assigned_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `assigned_at` | 是 | `string(date-time)` |  |  |  |
| `started_at` | 否 | `string \| null(date-time)` |  |  |  |
| `completed_at` | 否 | `string \| null(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_assignment/request.json`
- `mocks/http/get_assignment/response.json`
- `mocks/http/get_assignment/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_build_run` — 获取知识构建任务

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/build-runs/{build_run_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseBuildRun`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `BuildRunView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `status` | 是 | `string` | queued, running, succeeded, failed, cancelled |  |  |
| `current_stage` | 是 | `string` | queued, analyzing, parsing, chunking, indexing, evaluating, publishing, completed |  |  |
| `progress_percent` | 是 | `integer` |  |  |  |
| `selected_strategy` | 否 | `string \| null` | hybrid_rag, vector_rag, graph_rag, None |  |  |
| `error_code` | 否 | `string \| null` |  |  |  |
| `error_message` | 否 | `string \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `finished_at` | 否 | `string \| null(date-time)` |  |  |  |
| `requested_document_ids` | 否 | `array<string(uuid)>` |  |  |  |
| `build_mode` | 否 | `string` | full, incremental |  |  |
| `build_status` | 否 | `string` | queued, analyzing, parsing, chunking, extracting_entities, indexing, evaluating, succeeded, failed, cancelled |  |  |
| `current_stage_code` | 否 | `string` |  |  |  |
| `target_version_id` | 否 | `string \| null(uuid)` |  |  |  |
| `requested_by_user_id` | 否 | `string(uuid)` |  |  |  |
| `started_at` | 否 | `string \| null(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_build_run/request.json`
- `mocks/http/get_build_run/response.json`
- `mocks/http/get_build_run/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_document` — 获取文档

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/documents/{document_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseDocument`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `DocumentView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `file_name` | 是 | `string` |  |  |  |
| `file_size_bytes` | 否 | `integer` |  |  |  |
| `mime_type` | 否 | `string` |  |  |  |
| `status` | 是 | `string` | uploaded, queued, parsing, indexed, failed, deleted |  |  |
| `error_code` | 否 | `string \| null` |  |  |  |
| `error_message` | 否 | `string \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 否 | `string(date-time)` |  |  |  |
| `original_file_name` | 否 | `string` |  |  |  |
| `stored_file_name` | 否 | `string` |  |  |  |
| `file_extension` | 否 | `string` |  |  |  |
| `content_sha256` | 否 | `string` |  |  |  |
| `document_status` | 否 | `string` | uploaded, parsing, parsed, parse_failed, indexed, archived |  |  |
| `parse_error_code` | 否 | `string \| null` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_document/request.json`
- `mocks/http/get_document/response.json`
- `mocks/http/get_document/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_knowledge_base` — 获取知识库

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseKnowledgeBase`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeBaseView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `name` | 是 | `string` |  |  |  |
| `description` | 否 | `string \| null` |  |  |  |
| `status` | 是 | `string` | draft, building, ready, failed, archived |  |  |
| `document_count` | 是 | `integer` |  | 文档数 |  |
| `active_version` | 否 | `integer \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  | 更新时间 |  |
| `active_version_id` | 否 | `string \| null(uuid)` |  | 当前激活版本 |  |
| `active_version_no` | 否 | `integer \| null` |  | 当前激活版本号 |  |

**必须使用的 Mock**

- `mocks/http/get_knowledge_base/request.json`
- `mocks/http/get_knowledge_base/response.json`
- `mocks/http/get_knowledge_base/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_knowledge_base_version` — 获取知识库版本

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions/{knowledge_base_version_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `path:knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseKnowledgeBaseVersion`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeBaseVersionView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `version_no` | 是 | `integer` |  |  |  |
| `version_status` | 是 | `string` | candidate, quality_failed, ready, published, retired |  |  |
| `source_build_run_id` | 是 | `string(uuid)` |  |  |  |
| `chunk_count` | 是 | `integer` |  |  |  |
| `knowledge_node_count` | 是 | `integer` |  |  |  |
| `quality_score` | 是 | `number` |  |  |  |
| `published_at` | 否 | `string \| null(date-time)` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_knowledge_base_version/request.json`
- `mocks/http/get_knowledge_base_version/response.json`
- `mocks/http/get_knowledge_base_version/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_knowledge_graph` — 获取知识图谱摘要

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions/{knowledge_base_version_id}/knowledge-graph`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨、陈尧
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `path:knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseKnowledgeGraph`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeGraphView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `node_count` | 是 | `integer` |  |  |  |
| `edge_count` | 是 | `integer` |  |  |  |
| `nodes` | 是 | `array<object>` |  |  |  |
| `nodes[].knowledge_node_id` | 是 | `string` |  |  |  |
| `nodes[].node_name` | 是 | `string` |  |  |  |
| `nodes[].node_type` | 是 | `string` |  |  |  |
| `nodes[].importance_score` | 否 | `number` |  |  |  |
| `edges` | 是 | `array<object>` |  |  |  |
| `edges[].edge_id` | 是 | `string` |  |  |  |
| `edges[].source_knowledge_node_id` | 是 | `string` |  |  |  |
| `edges[].target_knowledge_node_id` | 是 | `string` |  |  |  |
| `edges[].relation_type` | 是 | `string` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_knowledge_graph/request.json`
- `mocks/http/get_knowledge_graph/response.json`
- `mocks/http/get_knowledge_graph/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_learner` — 获取学习者

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/learners/{learner_id}`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseLearner`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `LearnerView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `user_id` | 是 | `string(uuid)` |  |  |  |
| `display_name` | 是 | `string` |  |  |  |
| `department_name` | 否 | `string \| null` |  |  |  |
| `job_role_name` | 否 | `string \| null` |  |  |  |
| `learner_status` | 是 | `string` | active, inactive |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_learner/request.json`
- `mocks/http/get_learner/response.json`
- `mocks/http/get_learner/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_quality_report` — 获取知识库质量报告

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions/{knowledge_base_version_id}/quality-report`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨、陈尧
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `path:knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseQualityReport`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `QualityReportView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `version` | 是 | `integer` |  |  |  |
| `overall_score` | 是 | `number` |  |  |  |
| `publishable` | 是 | `boolean` |  |  |  |
| `metrics` | 是 | `array<QualityMetric>` |  |  |  |
| `metrics[].metric_name` | 是 | `string` |  |  |  |
| `metrics[].metric_value` | 是 | `number` |  |  |  |
| `metrics[].threshold` | 是 | `number` |  |  |  |
| `metrics[].passed` | 是 | `boolean` |  |  |  |
| `blocking_issues` | 否 | `array<string>` |  |  |  |
| `generated_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_quality_report/request.json`
- `mocks/http/get_quality_report/response.json`
- `mocks/http/get_quality_report/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_training_program` — 获取培训项目

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/training-programs/{training_program_id}`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseTrainingProgram`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `TrainingProgramView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `program_name` | 是 | `string` |  |  |  |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `program_status` | 是 | `string` | draft, published, archived |  |  |
| `target_description` | 是 | `string` |  |  |  |
| `created_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_training_program/request.json`
- `mocks/http/get_training_program/response.json`
- `mocks/http/get_training_program/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_build_events` — 查询知识构建事件

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/build-runs/{build_run_id}/events`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |
| `query:after_sequence_no` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseBuildEventList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<BuildEventView>` |  |  |  |
| `items[].build_event_id` | 是 | `string(uuid)` |  |  |  |
| `items[].build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |
| `items[].sequence_no` | 是 | `integer` |  |  |  |
| `items[].stage_code` | 是 | `string` |  |  |  |
| `items[].event_type` | 是 | `string` | stage_started, progress_updated, warning, stage_completed, build_failed, build_succeeded |  |  |
| `items[].progress_percent` | 是 | `integer` |  |  |  |
| `items[].message` | 是 | `string` |  |  |  |
| `items[].occurred_at` | 是 | `string(date-time)` |  |  |  |
| `last_sequence_no` | 是 | `integer` |  |  |  |

**必须使用的 Mock**

- `mocks/http/list_build_events/request.json`
- `mocks/http/list_build_events/response.json`
- `mocks/http/list_build_events/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_build_runs` — 查询知识构建任务

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/build-runs`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseBuildRunList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<BuildRunView>` |  |  |  |
| `items[].build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |
| `items[].knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `items[].status` | 是 | `string` | queued, running, succeeded, failed, cancelled |  |  |
| `items[].current_stage` | 是 | `string` | queued, analyzing, parsing, chunking, indexing, evaluating, publishing, completed |  |  |
| `items[].progress_percent` | 是 | `integer` |  |  |  |
| `items[].selected_strategy` | 否 | `string \| null` | hybrid_rag, vector_rag, graph_rag, None |  |  |
| `items[].error_code` | 否 | `string \| null` |  |  |  |
| `items[].error_message` | 否 | `string \| null` |  |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `items[].finished_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].requested_document_ids` | 否 | `array<string(uuid)>` |  |  |  |
| `items[].build_mode` | 否 | `string` | full, incremental |  |  |
| `items[].build_status` | 否 | `string` | queued, analyzing, parsing, chunking, extracting_entities, indexing, evaluating, succeeded, failed, cancelled |  |  |
| `items[].current_stage_code` | 否 | `string` |  |  |  |
| `items[].target_version_id` | 否 | `string \| null(uuid)` |  |  |  |
| `items[].requested_by_user_id` | 否 | `string(uuid)` |  |  |  |
| `items[].started_at` | 否 | `string \| null(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_build_runs/request.json`
- `mocks/http/list_build_runs/response.json`
- `mocks/http/list_build_runs/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_documents` — 查询知识库文档

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/documents`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseDocumentList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<DocumentView>` |  |  |  |
| `items[].document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |
| `items[].knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `items[].file_name` | 是 | `string` |  |  |  |
| `items[].file_size_bytes` | 否 | `integer` |  |  |  |
| `items[].mime_type` | 否 | `string` |  |  |  |
| `items[].status` | 是 | `string` | uploaded, queued, parsing, indexed, failed, deleted |  |  |
| `items[].error_code` | 否 | `string \| null` |  |  |  |
| `items[].error_message` | 否 | `string \| null` |  |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `items[].updated_at` | 否 | `string(date-time)` |  |  |  |
| `items[].original_file_name` | 否 | `string` |  |  |  |
| `items[].stored_file_name` | 否 | `string` |  |  |  |
| `items[].file_extension` | 否 | `string` |  |  |  |
| `items[].content_sha256` | 否 | `string` |  |  |  |
| `items[].document_status` | 否 | `string` | uploaded, parsing, parsed, parse_failed, indexed, archived |  |  |
| `items[].parse_error_code` | 否 | `string \| null` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_documents/request.json`
- `mocks/http/list_documents/response.json`
- `mocks/http/list_documents/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_knowledge_base_versions` — 查询知识库版本

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseKnowledgeBaseVersionList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<KnowledgeBaseVersionView>` |  |  |  |
| `items[].knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `items[].knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `items[].version_no` | 是 | `integer` |  |  |  |
| `items[].version_status` | 是 | `string` | candidate, quality_failed, ready, published, retired |  |  |
| `items[].source_build_run_id` | 是 | `string(uuid)` |  |  |  |
| `items[].chunk_count` | 是 | `integer` |  |  |  |
| `items[].knowledge_node_count` | 是 | `integer` |  |  |  |
| `items[].quality_score` | 是 | `number` |  |  |  |
| `items[].published_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_knowledge_base_versions/request.json`
- `mocks/http/list_knowledge_base_versions/response.json`
- `mocks/http/list_knowledge_base_versions/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_knowledge_bases` — 查询知识库

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/knowledge-bases`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseKnowledgeBaseList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<KnowledgeBaseView>` |  |  |  |
| `items[].knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `items[].name` | 是 | `string` |  |  |  |
| `items[].description` | 否 | `string \| null` |  |  |  |
| `items[].status` | 是 | `string` | draft, building, ready, failed, archived |  |  |
| `items[].document_count` | 是 | `integer` |  | 文档数 |  |
| `items[].active_version` | 否 | `integer \| null` |  |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `items[].updated_at` | 是 | `string(date-time)` |  | 更新时间 |  |
| `items[].active_version_id` | 否 | `string \| null(uuid)` |  | 当前激活版本 |  |
| `items[].active_version_no` | 否 | `integer \| null` |  | 当前激活版本号 |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_knowledge_bases/request.json`
- `mocks/http/list_knowledge_bases/response.json`
- `mocks/http/list_knowledge_bases/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_learner_assignments` — 查询学习者培训任务

- 你的关系：**实现负责人**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/learners/{learner_id}/assignments`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：X、斯汀
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseLearnerAssignmentList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<LearnerAssignmentView>` |  |  |  |
| `items[].assignment_id` | 是 | `string(uuid)` |  | 培训分配标识 | 杨欣怡 |
| `items[].organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `items[].training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `items[].learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `items[].assignment_status` | 是 | `string` | assigned, in_progress, completed, cancelled, expired |  |  |
| `items[].due_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].assigned_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `items[].assigned_at` | 是 | `string(date-time)` |  |  |  |
| `items[].started_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].completed_at` | 否 | `string \| null(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_learner_assignments/request.json`
- `mocks/http/list_learner_assignments/response.json`
- `mocks/http/list_learner_assignments/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_learners` — 查询学习者

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/learners`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseLearnerList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<LearnerView>` |  |  |  |
| `items[].learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `items[].organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `items[].user_id` | 是 | `string(uuid)` |  |  |  |
| `items[].display_name` | 是 | `string` |  |  |  |
| `items[].department_name` | 否 | `string \| null` |  |  |  |
| `items[].job_role_name` | 否 | `string \| null` |  |  |  |
| `items[].learner_status` | 是 | `string` | active, inactive |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_learners/request.json`
- `mocks/http/list_learners/response.json`
- `mocks/http/list_learners/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_program_assignments` — 查询培训项目分配

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/training-programs/{training_program_id}/assignments`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseLearnerAssignmentList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<LearnerAssignmentView>` |  |  |  |
| `items[].assignment_id` | 是 | `string(uuid)` |  | 培训分配标识 | 杨欣怡 |
| `items[].organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `items[].training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `items[].learner_id` | 是 | `string(uuid)` |  | 学习者标识 | 杨欣怡 |
| `items[].assignment_status` | 是 | `string` | assigned, in_progress, completed, cancelled, expired |  |  |
| `items[].due_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].assigned_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `items[].assigned_at` | 是 | `string(date-time)` |  |  |  |
| `items[].started_at` | 否 | `string \| null(date-time)` |  |  |  |
| `items[].completed_at` | 否 | `string \| null(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_program_assignments/request.json`
- `mocks/http/list_program_assignments/response.json`
- `mocks/http/list_program_assignments/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_training_programs` — 查询培训项目

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/training-programs`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseTrainingProgramList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<TrainingProgramView>` |  |  |  |
| `items[].training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `items[].organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `items[].program_name` | 是 | `string` |  |  |  |
| `items[].knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `items[].knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `items[].program_status` | 是 | `string` | draft, published, archived |  |  |
| `items[].target_description` | 是 | `string` |  |  |  |
| `items[].created_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `items[].updated_at` | 是 | `string(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_training_programs/request.json`
- `mocks/http/list_training_programs/response.json`
- `mocks/http/list_training_programs/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `publish_training_program` — 发布培训项目

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/training-programs/{training_program_id}/publish`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |

**请求体结构：`无请求体`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `idempotency_key` | 是 | `string` |  | 避免重复创建或重复提交的幂等键 | 张英赫 |

**成功响应结构：`ApiResponseTrainingProgram`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `TrainingProgramView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `program_name` | 是 | `string` |  |  |  |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `program_status` | 是 | `string` | draft, published, archived |  |  |
| `target_description` | 是 | `string` |  |  |  |
| `created_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/publish_training_program/request.json`
- `mocks/http/publish_training_program/response.json`
- `mocks/http/publish_training_program/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `retry_document_parse` — 重试文档解析

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/documents/{document_id}/retry-parse`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |

**请求体结构：`无请求体`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `idempotency_key` | 是 | `string` |  | 避免重复创建或重复提交的幂等键 | 张英赫 |

**成功响应结构：`ApiResponseDocument`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `DocumentView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `file_name` | 是 | `string` |  |  |  |
| `file_size_bytes` | 否 | `integer` |  |  |  |
| `mime_type` | 否 | `string` |  |  |  |
| `status` | 是 | `string` | uploaded, queued, parsing, indexed, failed, deleted |  |  |
| `error_code` | 否 | `string \| null` |  |  |  |
| `error_message` | 否 | `string \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 否 | `string(date-time)` |  |  |  |
| `original_file_name` | 否 | `string` |  |  |  |
| `stored_file_name` | 否 | `string` |  |  |  |
| `file_extension` | 否 | `string` |  |  |  |
| `content_sha256` | 否 | `string` |  |  |  |
| `document_status` | 否 | `string` | uploaded, parsing, parsed, parse_failed, indexed, archived |  |  |
| `parse_error_code` | 否 | `string \| null` |  |  |  |

**必须使用的 Mock**

- `mocks/http/retry_document_parse/request.json`
- `mocks/http/retry_document_parse/response.json`
- `mocks/http/retry_document_parse/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `update_knowledge_base` — 更新知识库

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`PATCH /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |

**请求体结构：`无请求体`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_name` | 否 | `string \| null` |  |  |  |
| `description` | 否 | `string \| null` |  |  |  |
| `knowledge_base_status` | 否 | `string \| null` |  |  |  |

**成功响应结构：`ApiResponseKnowledgeBase`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeBaseView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `name` | 是 | `string` |  |  |  |
| `description` | 否 | `string \| null` |  |  |  |
| `status` | 是 | `string` | draft, building, ready, failed, archived |  |  |
| `document_count` | 是 | `integer` |  | 文档数 |  |
| `active_version` | 否 | `integer \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  | 更新时间 |  |
| `active_version_id` | 否 | `string \| null(uuid)` |  | 当前激活版本 |  |
| `active_version_no` | 否 | `integer \| null` |  | 当前激活版本号 |  |

**必须使用的 Mock**

- `mocks/http/update_knowledge_base/request.json`
- `mocks/http/update_knowledge_base/response.json`
- `mocks/http/update_knowledge_base/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `update_training_program` — 更新培训项目

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`PATCH /api/v1/organizations/{organization_id}/training-programs/{training_program_id}`
- 契约负责人：杨欣怡
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |

**请求体结构：`UpdateTrainingProgramRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `program_name` | 否 | `string \| null` |  |  |  |
| `knowledge_base_version_id` | 否 | `string \| null(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `target_description` | 否 | `string \| null` |  |  |  |

**成功响应结构：`ApiResponseTrainingProgram`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `TrainingProgramView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `training_program_id` | 是 | `string(uuid)` |  | 培训项目标识 | 杨欣怡 |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `program_name` | 是 | `string` |  |  |  |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `program_status` | 是 | `string` | draft, published, archived |  |  |
| `target_description` | 是 | `string` |  |  |  |
| `created_by_user_id` | 是 | `string(uuid)` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/update_training_program/request.json`
- `mocks/http/update_training_program/response.json`
- `mocks/http/update_training_program/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `upload_document` — 上传文档

- 你的关系：**实现负责人、调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/documents`
- 契约负责人：李佳乐
- 实现负责人：陈严谨
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseDocumentUpload`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `DocumentUploadView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `document` | 是 | `DocumentView` |  |  |  |
| `document.document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |
| `document.knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `document.file_name` | 是 | `string` |  |  |  |
| `document.file_size_bytes` | 否 | `integer` |  |  |  |
| `document.mime_type` | 否 | `string` |  |  |  |
| `document.status` | 是 | `string` | uploaded, queued, parsing, indexed, failed, deleted |  |  |
| `document.error_code` | 否 | `string \| null` |  |  |  |
| `document.error_message` | 否 | `string \| null` |  |  |  |
| `document.created_at` | 是 | `string(date-time)` |  |  |  |
| `document.updated_at` | 否 | `string(date-time)` |  |  |  |
| `document.original_file_name` | 否 | `string` |  |  |  |
| `document.stored_file_name` | 否 | `string` |  |  |  |
| `document.file_extension` | 否 | `string` |  |  |  |
| `document.content_sha256` | 否 | `string` |  |  |  |
| `document.document_status` | 否 | `string` | uploaded, parsing, parsed, parse_failed, indexed, archived |  |  |
| `document.parse_error_code` | 否 | `string \| null` |  |  |  |
| `duplicate_detected` | 是 | `boolean` |  |  |  |

**必须使用的 Mock**

- `mocks/http/upload_document/request.json`
- `mocks/http/upload_document/response.json`
- `mocks/http/upload_document/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `activate_knowledge_base_version` — 激活知识库版本

- 你的关系：**调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions/{knowledge_base_version_id}/activate`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `path:knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |

**请求体结构：`无请求体`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `idempotency_key` | 是 | `string` |  | 避免重复创建或重复提交的幂等键 | 张英赫 |

**成功响应结构：`ApiResponseKnowledgeBase`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeBaseView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `name` | 是 | `string` |  |  |  |
| `description` | 否 | `string \| null` |  |  |  |
| `status` | 是 | `string` | draft, building, ready, failed, archived |  |  |
| `document_count` | 是 | `integer` |  | 文档数 |  |
| `active_version` | 否 | `integer \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `updated_at` | 是 | `string(date-time)` |  | 更新时间 |  |
| `active_version_id` | 否 | `string \| null(uuid)` |  | 当前激活版本 |  |
| `active_version_no` | 否 | `integer \| null` |  | 当前激活版本号 |  |

**必须使用的 Mock**

- `mocks/http/activate_knowledge_base_version/request.json`
- `mocks/http/activate_knowledge_base_version/response.json`
- `mocks/http/activate_knowledge_base_version/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `cancel_build_run` — 取消知识构建任务

- 你的关系：**调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/build-runs/{build_run_id}/cancel`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |

**请求体结构：`无请求体`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `idempotency_key` | 是 | `string` |  | 避免重复创建或重复提交的幂等键 | 张英赫 |

**成功响应结构：`ApiResponseBuildRun`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `BuildRunView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `status` | 是 | `string` | queued, running, succeeded, failed, cancelled |  |  |
| `current_stage` | 是 | `string` | queued, analyzing, parsing, chunking, indexing, evaluating, publishing, completed |  |  |
| `progress_percent` | 是 | `integer` |  |  |  |
| `selected_strategy` | 否 | `string \| null` | hybrid_rag, vector_rag, graph_rag, None |  |  |
| `error_code` | 否 | `string \| null` |  |  |  |
| `error_message` | 否 | `string \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `finished_at` | 否 | `string \| null(date-time)` |  |  |  |
| `requested_document_ids` | 否 | `array<string(uuid)>` |  |  |  |
| `build_mode` | 否 | `string` | full, incremental |  |  |
| `build_status` | 否 | `string` | queued, analyzing, parsing, chunking, extracting_entities, indexing, evaluating, succeeded, failed, cancelled |  |  |
| `current_stage_code` | 否 | `string` |  |  |  |
| `target_version_id` | 否 | `string \| null(uuid)` |  |  |  |
| `requested_by_user_id` | 否 | `string(uuid)` |  |  |  |
| `started_at` | 否 | `string \| null(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/cancel_build_run/request.json`
- `mocks/http/cancel_build_run/response.json`
- `mocks/http/cancel_build_run/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `create_build_run` — 发起知识构建

- 你的关系：**调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/build-runs`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |

**请求体结构：`CreateBuildRunRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `document_ids` | 是 | `array<string(uuid)>` |  |  |  |
| `strategy_mode` | 否 | `string` | auto, hybrid_rag, vector_rag, graph_rag |  |  |

**成功响应结构：`ApiResponseBuildRun`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `BuildRunView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `build_run_id` | 是 | `string(uuid)` |  | 知识构建任务标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `status` | 是 | `string` | queued, running, succeeded, failed, cancelled |  |  |
| `current_stage` | 是 | `string` | queued, analyzing, parsing, chunking, indexing, evaluating, publishing, completed |  |  |
| `progress_percent` | 是 | `integer` |  |  |  |
| `selected_strategy` | 否 | `string \| null` | hybrid_rag, vector_rag, graph_rag, None |  |  |
| `error_code` | 否 | `string \| null` |  |  |  |
| `error_message` | 否 | `string \| null` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `finished_at` | 否 | `string \| null(date-time)` |  |  |  |
| `requested_document_ids` | 否 | `array<string(uuid)>` |  |  |  |
| `build_mode` | 否 | `string` | full, incremental |  |  |
| `build_status` | 否 | `string` | queued, analyzing, parsing, chunking, extracting_entities, indexing, evaluating, succeeded, failed, cancelled |  |  |
| `current_stage_code` | 否 | `string` |  |  |  |
| `target_version_id` | 否 | `string \| null(uuid)` |  |  |  |
| `requested_by_user_id` | 否 | `string(uuid)` |  |  |  |
| `started_at` | 否 | `string \| null(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/create_build_run/request.json`
- `mocks/http/create_build_run/response.json`
- `mocks/http/create_build_run/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_current_user` — 获取当前用户

- 你的关系：**调用/展示方**
- 方法与路径：`GET /api/v1/me`
- 契约负责人：张英赫
- 实现负责人：张英赫
- 展示负责人：无
- 使用成员：X、斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

无

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseUser`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `UserView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `user_id` | 是 | `string(uuid)` |  | 用户标识 |  |
| `organization_id` | 是 | `string(uuid)` |  | 组织标识 | 张英赫 |
| `display_name` | 是 | `string` |  | 显示名称 |  |
| `email` | 是 | `string` |  | 邮箱 |  |
| `role_code` | 是 | `string` | platform_admin, organization_admin, trainer, reviewer, learner | 角色编码 |  |
| `learner_id` | 否 | `string \| null(uuid)` |  | 学习者标识 | 杨欣怡 |
| `account_status` | 是 | `string` | active, disabled, pending | 账号状态 |  |

**必须使用的 Mock**

- `mocks/http/get_current_user/request.json`
- `mocks/http/get_current_user/response.json`
- `mocks/http/get_current_user/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_human_review_task` — 获取人工审核任务

- 你的关系：**调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/human-review-tasks/{human_review_task_id}`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：陈尧
- 使用成员：斯汀、陈严谨、陈尧
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:human_review_task_id` | 是 | `string(uuid)` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseHumanReviewTask`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `HumanReviewTaskView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `human_review_task_id` | 是 | `string(uuid)` |  |  |  |
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `session_id` | 是 | `string(uuid)` |  | 一次完整学习会话标识 | 张英赫 |
| `decision_cycle_id` | 是 | `string(uuid)` |  | 会话内一轮联合决策标识 | 张英赫 |
| `task_type` | 是 | `string` | knowledge_quality, resource_accuracy, assessment_ambiguity, decision_conflict, privacy_risk |  |  |
| `reference_id` | 是 | `string` |  |  |  |
| `priority_level` | 是 | `string` | low, normal, high, urgent |  |  |
| `task_status` | 是 | `string` | open, assigned, resolved, cancelled |  |  |
| `assigned_reviewer_user_id` | 否 | `string \| null(uuid)` |  |  |  |
| `issue_summary` | 是 | `string` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |
| `resolved_at` | 否 | `string \| null(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_human_review_task/request.json`
- `mocks/http/get_human_review_task/response.json`
- `mocks/http/get_human_review_task/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `get_organization` — 获取组织信息

- 你的关系：**调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}`
- 契约负责人：张英赫
- 实现负责人：张英赫
- 展示负责人：无
- 使用成员：X、斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseOrganization`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `OrganizationView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `organization_name` | 是 | `string` |  |  |  |
| `organization_status` | 是 | `string` | active, suspended |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/get_organization/request.json`
- `mocks/http/get_organization/response.json`
- `mocks/http/get_organization/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `list_human_review_tasks` — 查询人工审核任务

- 你的关系：**调用/展示方**
- 方法与路径：`GET /api/v1/organizations/{organization_id}/human-review-tasks`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：陈尧
- 使用成员：斯汀、陈严谨、陈尧
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `query:page_no` | 否 | `integer` |  |  |  |
| `query:page_size` | 否 | `integer` |  |  |  |

**请求体结构：`无请求体`**

无

**成功响应结构：`ApiResponseHumanReviewTaskList`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `object` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `items` | 是 | `array<HumanReviewTaskView>` |  |  |  |
| `items[].human_review_task_id` | 是 | `string(uuid)` |  |  |  |
| `items[].organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `items[].session_id` | 是 | `string(uuid)` |  | 一次完整学习会话标识 | 张英赫 |
| `items[].decision_cycle_id` | 是 | `string(uuid)` |  | 会话内一轮联合决策标识 | 张英赫 |
| `items[].task_type` | 是 | `string` | knowledge_quality, resource_accuracy, assessment_ambiguity, decision_conflict, privacy_risk |  |  |
| `items[].reference_id` | 是 | `string` |  |  |  |
| `items[].priority_level` | 是 | `string` | low, normal, high, urgent |  |  |
| `items[].task_status` | 是 | `string` | open, assigned, resolved, cancelled |  |  |
| `items[].assigned_reviewer_user_id` | 否 | `string \| null(uuid)` |  |  |  |
| `items[].issue_summary` | 是 | `string` |  |  |  |
| `items[].created_at` | 是 | `string(date-time)` |  |  |  |
| `items[].resolved_at` | 否 | `string \| null(date-time)` |  |  |  |
| `page` | 是 | `PageMeta` |  |  |  |
| `page.page_no` | 是 | `integer` |  | 页码 |  |
| `page.page_size` | 是 | `integer` |  | 每页数量 |  |
| `page.total_count` | 是 | `integer` |  | 总记录数 |  |
| `page.has_next` | 是 | `boolean` |  | 是否有下一页 |  |

**必须使用的 Mock**

- `mocks/http/list_human_review_tasks/request.json`
- `mocks/http/list_human_review_tasks/response.json`
- `mocks/http/list_human_review_tasks/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `login` — 用户登录

- 你的关系：**调用/展示方**
- 方法与路径：`POST /api/v1/auth/login`
- 契约负责人：张英赫
- 实现负责人：张英赫
- 展示负责人：无
- 使用成员：X、斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

无

**请求体结构：`LoginRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `email` | 是 | `string` |  | 邮箱 |  |
| `password` | 是 | `string` |  | 密码 |  |

**成功响应结构：`ApiResponseToken`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `TokenView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `access_token` | 是 | `string` |  |  |  |
| `refresh_token` | 是 | `string` |  |  |  |
| `token_type` | 是 | `string` | bearer |  |  |
| `expires_in_seconds` | 是 | `integer` |  |  |  |
| `user` | 是 | `UserView` |  |  |  |
| `user.user_id` | 是 | `string(uuid)` |  | 用户标识 |  |
| `user.organization_id` | 是 | `string(uuid)` |  | 组织标识 | 张英赫 |
| `user.display_name` | 是 | `string` |  | 显示名称 |  |
| `user.email` | 是 | `string` |  | 邮箱 |  |
| `user.role_code` | 是 | `string` | platform_admin, organization_admin, trainer, reviewer, learner | 角色编码 |  |
| `user.learner_id` | 否 | `string \| null(uuid)` |  | 学习者标识 | 杨欣怡 |
| `user.account_status` | 是 | `string` | active, disabled, pending | 账号状态 |  |

**必须使用的 Mock**

- `mocks/http/login/request.json`
- `mocks/http/login/response.json`
- `mocks/http/login/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `publish_knowledge_base_version` — 发布知识库版本

- 你的关系：**调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions/{knowledge_base_version_id}/publish`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `path:knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |

**请求体结构：`无请求体`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `idempotency_key` | 是 | `string` |  | 避免重复创建或重复提交的幂等键 | 张英赫 |

**成功响应结构：`ApiResponseKnowledgeBaseVersion`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `KnowledgeBaseVersionView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `version_no` | 是 | `integer` |  |  |  |
| `version_status` | 是 | `string` | candidate, quality_failed, ready, published, retired |  |  |
| `source_build_run_id` | 是 | `string(uuid)` |  |  |  |
| `chunk_count` | 是 | `integer` |  |  |  |
| `knowledge_node_count` | 是 | `integer` |  |  |  |
| `quality_score` | 是 | `number` |  |  |  |
| `published_at` | 否 | `string \| null(date-time)` |  |  |  |
| `created_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/publish_knowledge_base_version/request.json`
- `mocks/http/publish_knowledge_base_version/response.json`
- `mocks/http/publish_knowledge_base_version/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。

### `run_retrieval_test` — 运行检索评测

- 你的关系：**调用/展示方**
- 方法与路径：`POST /api/v1/organizations/{organization_id}/knowledge-bases/{knowledge_base_id}/versions/{knowledge_base_version_id}/retrieval-tests`
- 契约负责人：李佳乐
- 实现负责人：李佳乐
- 展示负责人：无
- 使用成员：斯汀、陈严谨
- 契约状态：`frozen_v6`

**路径与查询变量**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `path:organization_id` | 是 | `string(uuid)` |  | 组织租户标识 | 张英赫 |
| `path:knowledge_base_id` | 是 | `string(uuid)` |  | 逻辑知识库标识 | 李佳乐 |
| `path:knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |

**请求体结构：`RunRetrievalTestRequest`**

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `test_cases` | 是 | `array<RetrievalTestCaseInput>` |  |  |  |
| `test_cases[].test_case_id` | 是 | `string` |  |  |  |
| `test_cases[].query_text` | 是 | `string` |  |  |  |
| `test_cases[].expected_knowledge_node_ids` | 否 | `array<string>` |  |  |  |
| `test_cases[].expected_evidence_ids` | 否 | `array<string>` |  |  |  |

**成功响应结构：`ApiResponseRetrievalTestResult`**

统一响应外壳：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `code` | 是 | `string` |  |  |  |
| `message` | 是 | `string` |  |  |  |
| `data` | 是 | `RetrievalTestResultView` |  |  |  |
| `meta` | 是 | `ResponseMeta` |  |  |  |

响应 `data` 中的主要跨模块字段：

| 字段 | 必填 | 类型 | 枚举/限制 | 含义 | 字段负责人 |
|---|---|---|---|---|---|
| `retrieval_test_run_id` | 是 | `string(uuid)` |  |  |  |
| `knowledge_base_version_id` | 是 | `string(uuid)` |  | 不可变知识库版本标识 | 李佳乐 |
| `hit_rate_at_k` | 是 | `number` |  |  |  |
| `mean_reciprocal_rank` | 是 | `number` |  |  |  |
| `case_results` | 是 | `array<object>` |  |  |  |
| `case_results[].test_case_id` | 是 | `string` |  |  |  |
| `case_results[].passed` | 是 | `boolean` |  |  |  |
| `case_results[].hits` | 是 | `array<RetrievalHitView>` |  |  |  |
| `case_results[].hits[].evidence_id` | 是 | `string` |  | 可追溯证据标识 | 李佳乐 |
| `case_results[].hits[].document_id` | 是 | `string(uuid)` |  | 上传文档标识 | 李佳乐 |
| `case_results[].hits[].chunk_id` | 是 | `string` |  |  |  |
| `case_results[].hits[].knowledge_node_id` | 否 | `string \| null` |  |  |  |
| `case_results[].hits[].rank_no` | 是 | `integer` |  |  |  |
| `case_results[].hits[].relevance_score` | 是 | `number` |  |  |  |
| `case_results[].hits[].citation_text` | 是 | `string` |  |  |  |
| `completed_at` | 是 | `string(date-time)` |  |  |  |

**必须使用的 Mock**

- `mocks/http/run_retrieval_test/request.json`
- `mocks/http/run_retrieval_test/response.json`
- `mocks/http/run_retrieval_test/error_response.json`

> 实现时不得根据页面便利改字段。若当前字段无法满足真实业务，先提交接口变更申请。


## 7. 逐个智能体内部接口说明

本成员没有直接智能体接口，但仍需遵守公开接口和跨模块字段契约。

## 8. 你的 Mock 已经固化在哪里

使用规则：

1. 页面开发先读取 HTTP `response.json` 的 `body`。
2. 后端实现用 `request.json` 验证输入，用 `response.json` 验证输出。
3. 智能体开发用对应 `request.json` 与 `response.json`。
4. 错误页面至少覆盖 `error_response.json`。
5. 禁止把 Mock 中的示例 UUID、文案或分数当成业务常量写死。
6. 真实接口完成后，只切换数据源，不改页面字段。

### HTTP 接口 Mock

- `mocks/http/activate_knowledge_base_version/error_response.json`
- `mocks/http/activate_knowledge_base_version/request.json`
- `mocks/http/activate_knowledge_base_version/response.json`
- `mocks/http/archive_document/error_response.json`
- `mocks/http/archive_document/request.json`
- `mocks/http/archive_document/response.json`
- `mocks/http/archive_knowledge_base/error_response.json`
- `mocks/http/archive_knowledge_base/request.json`
- `mocks/http/archive_knowledge_base/response.json`
- `mocks/http/cancel_build_run/error_response.json`
- `mocks/http/cancel_build_run/request.json`
- `mocks/http/cancel_build_run/response.json`
- `mocks/http/create_assignments/error_response.json`
- `mocks/http/create_assignments/request.json`
- `mocks/http/create_assignments/response.json`
- `mocks/http/create_build_run/error_response.json`
- `mocks/http/create_build_run/request.json`
- `mocks/http/create_build_run/response.json`
- `mocks/http/create_knowledge_base/error_response.json`
- `mocks/http/create_knowledge_base/request.json`
- `mocks/http/create_knowledge_base/response.json`
- `mocks/http/create_training_program/error_response.json`
- `mocks/http/create_training_program/request.json`
- `mocks/http/create_training_program/response.json`
- `mocks/http/get_assignment/error_response.json`
- `mocks/http/get_assignment/request.json`
- `mocks/http/get_assignment/response.json`
- `mocks/http/get_build_run/error_response.json`
- `mocks/http/get_build_run/request.json`
- `mocks/http/get_build_run/response.json`
- `mocks/http/get_current_user/error_response.json`
- `mocks/http/get_current_user/request.json`
- `mocks/http/get_current_user/response.json`
- `mocks/http/get_document/error_response.json`
- `mocks/http/get_document/request.json`
- `mocks/http/get_document/response.json`
- `mocks/http/get_human_review_task/error_response.json`
- `mocks/http/get_human_review_task/request.json`
- `mocks/http/get_human_review_task/response.json`
- `mocks/http/get_knowledge_base/error_response.json`
- `mocks/http/get_knowledge_base/request.json`
- `mocks/http/get_knowledge_base/response.json`
- `mocks/http/get_knowledge_base_version/error_response.json`
- `mocks/http/get_knowledge_base_version/request.json`
- `mocks/http/get_knowledge_base_version/response.json`
- `mocks/http/get_knowledge_graph/error_response.json`
- `mocks/http/get_knowledge_graph/request.json`
- `mocks/http/get_knowledge_graph/response.json`
- `mocks/http/get_learner/error_response.json`
- `mocks/http/get_learner/request.json`
- `mocks/http/get_learner/response.json`
- `mocks/http/get_organization/error_response.json`
- `mocks/http/get_organization/request.json`
- `mocks/http/get_organization/response.json`
- `mocks/http/get_quality_report/error_response.json`
- `mocks/http/get_quality_report/request.json`
- `mocks/http/get_quality_report/response.json`
- `mocks/http/get_training_program/error_response.json`
- `mocks/http/get_training_program/request.json`
- `mocks/http/get_training_program/response.json`
- `mocks/http/list_build_events/error_response.json`
- `mocks/http/list_build_events/request.json`
- `mocks/http/list_build_events/response.json`
- `mocks/http/list_build_runs/error_response.json`
- `mocks/http/list_build_runs/request.json`
- `mocks/http/list_build_runs/response.json`
- `mocks/http/list_documents/error_response.json`
- `mocks/http/list_documents/request.json`
- `mocks/http/list_documents/response.json`
- `mocks/http/list_human_review_tasks/error_response.json`
- `mocks/http/list_human_review_tasks/request.json`
- `mocks/http/list_human_review_tasks/response.json`
- `mocks/http/list_knowledge_base_versions/error_response.json`
- `mocks/http/list_knowledge_base_versions/request.json`
- `mocks/http/list_knowledge_base_versions/response.json`
- `mocks/http/list_knowledge_bases/error_response.json`
- `mocks/http/list_knowledge_bases/request.json`
- `mocks/http/list_knowledge_bases/response.json`
- `mocks/http/list_learner_assignments/error_response.json`
- `mocks/http/list_learner_assignments/request.json`
- `mocks/http/list_learner_assignments/response.json`
- `mocks/http/list_learners/error_response.json`
- `mocks/http/list_learners/request.json`
- `mocks/http/list_learners/response.json`
- `mocks/http/list_program_assignments/error_response.json`
- `mocks/http/list_program_assignments/request.json`
- `mocks/http/list_program_assignments/response.json`
- `mocks/http/list_training_programs/error_response.json`
- `mocks/http/list_training_programs/request.json`
- `mocks/http/list_training_programs/response.json`
- `mocks/http/login/error_response.json`
- `mocks/http/login/request.json`
- `mocks/http/login/response.json`
- `mocks/http/publish_knowledge_base_version/error_response.json`
- `mocks/http/publish_knowledge_base_version/request.json`
- `mocks/http/publish_knowledge_base_version/response.json`
- `mocks/http/publish_training_program/error_response.json`
- `mocks/http/publish_training_program/request.json`
- `mocks/http/publish_training_program/response.json`
- `mocks/http/retry_document_parse/error_response.json`
- `mocks/http/retry_document_parse/request.json`
- `mocks/http/retry_document_parse/response.json`
- `mocks/http/run_retrieval_test/error_response.json`
- `mocks/http/run_retrieval_test/request.json`
- `mocks/http/run_retrieval_test/response.json`
- `mocks/http/update_knowledge_base/error_response.json`
- `mocks/http/update_knowledge_base/request.json`
- `mocks/http/update_knowledge_base/response.json`
- `mocks/http/update_training_program/error_response.json`
- `mocks/http/update_training_program/request.json`
- `mocks/http/update_training_program/response.json`
- `mocks/http/upload_document/error_response.json`
- `mocks/http/upload_document/request.json`
- `mocks/http/upload_document/response.json`
### 完整流程 Mock

- `mocks/workflows/admin_knowledge_build_flow.json`

## 9. 分阶段开发任务

1. 阶段0：学习管理端接口切片和Mock
2. 阶段1：实现知识库、文档、构建状态Mock页面
3. 阶段2：实现批准的简单接口并联调真实构建
4. 阶段3：实现学习者、培训项目和分配页面
5. 阶段4：管理端只读查看资源与测评记录
6. 阶段5：人工审核和智能体运行记录只读页面
7. 阶段6：错误状态与回归修复
8. 阶段7：演示数据检查

每个阶段只能在上一个阶段验收通过后进入下一阶段。页面成员优先完成 Mock 闭环；智能体成员优先完成 Schema 校验和确定性 Mock，再接真实模型。

## 10. 每个任务的固定执行步骤

1. 从最新 `develop` 创建独立功能分支。
2. 阅读任务卡，确认允许修改目录、operation_id、Schema 和 Mock。
3. 先用 Mock 或确定性实现跑通，不直接连接真实模型。
4. 编写实现，不改变跨模块字段。
5. 覆盖成功、空数据、错误、超时/重试或重复提交情况。
6. 运行类型检查、单元测试、契约校验和构建。
7. 在 PR 中写明使用的 operation_id、Schema、Mock 和跨模块边界编号。
8. 审核人批准且自动测试通过后才能合并。

## 11. 给 AI 编码工具的固定提示词

将下面文字放在每次 Codex 或其他 AI 编码任务开头，再追加具体任务：

```text
你正在完成挑战杯项目中“管理端、知识库页面与明确范围的简单接口”相关任务。必须遵守以下规则：
1. 只修改任务卡允许的目录：apps/web/src/features/admin/**, services/knowledge/api/**, services/training/api/**, tests/admin/**。
2. HTTP 接口只使用本文件列出的 operation_id、方法、路径和字段；不得自行改名。
3. 智能体或内部服务输出必须通过指定 JSON Schema 校验。
4. 跨模块 JSON 字段使用 snake_case；TypeScript 类型名使用 PascalCase。
5. 不得修改 api/openapi.yaml、api/internal_agent_interfaces.yaml、contracts/jsonschema/，除非接口变更申请已批准。
6. 必须使用对应 Mock 完成正常、空数据、错误、超时/重试状态。
7. 输出代码后列出：修改文件、使用的 operation_id、使用的 Schema、运行的测试命令。
```

## 12. 提交前验收清单

- [ ] 当前分支不是 `main` 或 `develop`
- [ ] 只修改了允许目录和任务卡列出的文件
- [ ] 使用了正确的 operation_id、方法和路径
- [ ] 请求字段、响应字段、枚举值与契约完全一致
- [ ] 智能体/服务输出通过 JSON Schema 校验
- [ ] 没有把内部临时变量直接作为跨模块字段
- [ ] 没有把草稿编号当成正式编号
- [ ] 没有在前端泄露测评答案或内部推理原文
- [ ] 使用了指定 Mock，没有新建第二套 Mock
- [ ] 成功、空数据、错误、加载状态已测试
- [ ] 幂等、权限、组织隔离和异常恢复已按任务要求处理
- [ ] PR 已列出 operation_id、Schema、Mock、测试结果和截图/日志

## 13. 接口不够用时怎么办

不得直接改字段。填写 `templates/API_CHANGE_REQUEST.md`，至少说明：

- 当前 operation_id 或内部接口名称
- 现有字段为什么无法满足
- 建议新增、删除或修改的字段
- 生产方、消费方和受影响成员
- 对 OpenAPI、Schema、Mock、类型和数据库的影响
- 兼容方案与迁移方式

批准后按顺序修改：正式契约 → Schema → Mock → 类型 → 实现 → 测试。
